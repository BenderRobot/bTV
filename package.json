{
  "name": "btv",
  "private": true,
  "version": "1.0.0",
  "description": "App IPTV Tizen (bTV) — scripts de dev (tests, lint) uniquement, l'app elle-meme ne depend d'aucun build.",
  "scripts": {
    "test": "node --test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "globals": "^15.9.0"
  }
}
